# Dependencies
node_modules/
__pycache__/
*.pyc
.venv/
venv/
env/
.pnpm-store/

# Environment
.env
.env.local
.env.*.local

# Build output
dist/
build/
*.egg-info/
.eggs/

# Flutter
.dart_tool/
.flutter-plugins
.flutter-plugins-dependencies

# Firebase / Node
functions/lib/
functions/node_modules/
functions/.secret.local
firebase-debug.log
firebase-debug.*.log
.firebase/

# Scripts build output
scripts/lib/

# LarpManager fixture dumps captured with --no-scrub MUST NOT be committed.
# scripts/capture-lm-fixture.ts auto-appends '-unscrubbed' when --no-scrub is set.
functions/test/fixtures/*-unscrubbed/

# IDE & tools
.claude/
.cursor/history/
.cursor/chat/
.idea/
.vscode/
*.swp
*.swo

# AI / agent build infrastructure — not part of the RoleKeeper application.
# These files help build the app but are not the app itself, so they live
# locally and are intentionally not tracked in git.
tdd-mcp/
.cursor/mcp.json
.cursor/rules/tdd-worker.mdc
.cursor/rules/planner.mdc
.agent-context.md
docs/tasks/
SETUP.md
project-documentation/

# OS
.DS_Store
Thumbs.db

# Logs
*.log
npm-debug.log*
firepit-log.txt
