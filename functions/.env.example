# LarpManager integration is configured per game in the app (Home → LM Integration).
# Credentials are stored in Google Cloud Secret Manager (lm-auth-{gameId}), not here.
#
# Optional: other Firebase Functions env vars for your project follow:
# https://firebase.google.com/docs/functions/config-env
